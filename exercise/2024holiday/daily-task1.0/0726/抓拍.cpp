#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    int n;
    cin >> n;
    char c;
    ll x, y;
    cin >> x >> y;
    ll mxy = 
    for (int i = 0; i < n; i++) {
        cin >> x >> y >> c;

    }    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}