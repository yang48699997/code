#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve() {
    int n = 100, k = 5;
    vector<int> sg(101);
    sg[0] = 0;
    for (int i = 1; i <= k; i++) sg[i] = i;
    for (int i = )
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}