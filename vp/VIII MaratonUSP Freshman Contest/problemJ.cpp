#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve() {
    int x, y;
    cin >> x >> y;
    cout << (x - y) / 2 << "\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}