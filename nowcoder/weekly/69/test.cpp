#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve() {
    vector<int> a(2);
    cin >> a[0];
    cout << a[0];
}   

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}