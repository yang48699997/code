#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve() {
    vector a(10, vector<int> (10));
    cout << a[0][0] << '\n';
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
    return 0;
}